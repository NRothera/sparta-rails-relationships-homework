<%= form_with(model: network, local: true) do |form| %>
  <% if network.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(network.errors.count, "error") %> prohibited this network from being saved:</h2>

      <ul>
      <% network.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :brand %>
    <%= form.text_field :brand, id: :network_brand %>
  </div>

  <div class="field">
    <%= form.label :price %>
    <%= form.number_field :price, id: :network_price %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
